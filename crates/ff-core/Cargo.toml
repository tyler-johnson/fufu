[package]
name = "ff-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "fufu's engine: snapshots, the op journal, undo — on gitoxide"
publish = false

[dependencies]
gix.workspace = true
serde.workspace = true
serde_json.workspace = true
# The signer takes its payload as a file path and writes the signature beside
# it, so signing needs a scratch directory of its own.
tempfile.workspace = true
thiserror.workspace = true

[dev-dependencies]
ff-testsupport.workspace = true
