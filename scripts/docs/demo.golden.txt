
$ ff st
on main · 1 to sync
@  <id> <sha>   <age> ago
│  (no description)
│  M README.md +2  -0  ++++++++++++++++++++
│    1 file    +2  -0
●  —        <sha>   <age> ago
│  lexer: a token at a time

$ # a dirty working copy, but I need to switch branches

$ ff
@  <id> <sha>   <age> ago  ▸ [main]
│  (no description)
●  —        <sha>   <age> ago  ▸ [unicode-escapes]  (+ parked change, 2 files)
│  lexer: a token at a time
~

$ ff switch unicode-escapes
parked the open change on main (<sha>)
switched to unicode-escapes
resumed the parked change (2 file(s))
undo: ff undo

$ ff st
on unicode-escapes · base moved — fast-forwards
@  <id> <sha>   <age> ago
│  lexer: handle unicode escapes
│  M src/lexer.rs   +1  -0  +++++++
│  A src/unicode.rs +3  -0  ++++++++++++++++++++
│    2 files        +4  -0
●  —        <sha>   <age> ago
│  lexer: a token at a time

$ # the changes I made earlier on this branch...

$ # looks like it's ready to commit

$ ff commit
closed <sha> on unicode-escapes: lexer: handle unicode escapes (2 file(s))
undo: ff undo

$ # oops, forgot to add one thing

$ echo 'mod unicode;' >> src/lib.rs

$ ff absorb
absorbed into <sha>: lexer: handle unicode escapes
undo: ff undo

$ ff st
on unicode-escapes · base moved — rebases cleanly (1 commit replayed)
@  no changes
│  (no description)
●  —        <sha>   <age> ago
│  lexer: handle unicode escapes

$ # oh, main moved while I worked, so let's catch up

$ ff sync
fetching from origin
main moved ahead by 1 commit(s)
replayed 1 commit(s) onto main
updated the working tree (1 file(s))
not published yet — ff publish
main
    fast-forwarded to origin/main (1 commit(s))
undo: ff undo

$ # time to push my changes for review

$ ff publish
created origin/unicode-escapes and set unicode-escapes to track it
the push left the machine — ff undo cannot reach it
ff undo then ff publish rolls the shared copy back, under a lease

$ # what was I doing on main again?

$ ff switch main
switched to main
a parked change is waiting here but no longer applies cleanly:
  conflicts: README.md
it stays parked; resolve by hand with git stash, or continue working
undo: ff undo
