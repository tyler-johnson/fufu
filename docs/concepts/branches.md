# Branches

!!! note "Draft stub"
    Planned content, not yet written. Sources: help for `start`, `describe`, `branch`, `switch`.

To cover:

- Minted names: `ff start` forks from trunk under an anonymous petname (`ff/hidden-wren`); nothing to name until the work earns one.
- Claiming: `ff describe -b` renames with the capture chain, parked change, and pending description intact — what `git branch -m` would orphan.
- Every commit lands on a branch; HEAD stays attached — the [invariant](invariant.md) at work.
- `ff branch list`, deletion, and prefix matching in `ff switch`.
- Tracking: one branch, one shared copy; how `ff publish --to` records the answer.
