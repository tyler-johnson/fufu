# Worktrees

!!! note "Draft stub"
    Planned content, not yet written. Sources: help for `worktree`, `worktree-add`, `worktree-list`, `worktree-remove`, `watch`.

To cover:

- When a worktree beats a switch: long builds, an agent working in parallel, keeping a review checkout.
- `ff worktree add` / `list` / `remove`, and how parking interacts with multiple trees.
- One operation log across worktrees: what undo means when two trees share a repository.
- `ff watch` and capture in secondary worktrees.
