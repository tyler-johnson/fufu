# fufu vs git

!!! note "Draft stub"
    Planned content, not yet written.

What changes about your day, and the opinions you are accepting. To cover:

- What disappears: `add`, staging, stash, detached HEAD, `rebase -i`, the reflog as a recovery tool, fixup/autosquash.
- What replaces each, with the reflex mapping deferred to the [command table](command-table.md).
- The opinions (from DESIGN.md): branches rebase onto main rather than merging it in, unpublished commits are malleable, leased force-pushes are routine — and where the opinions stop (the push boundary).
- What is untouched: the repository format, remotes, forges, hooks, how work lands.
- Honest costs: a new mental model for undo, verbs that don't map one-to-one, trusting automatic capture.
