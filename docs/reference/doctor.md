# Doctor

!!! note "Draft stub"
    Planned content, not yet written. Source: help for `doctor`.

To cover:

- What `ff doctor` checks, lane by lane — including the raw git lane — and what each verdict means.
- Healthy output annotated, then the common failures and their fixes.
- When to run it: after adopting a repo, after a version bump, when something feels off.
